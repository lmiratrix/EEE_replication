#!/bin/bash

#SBATCH -n 1  #Number of cores

#SBATCH -t 40  #Runtime in minutes

#SBATCH -p stats #Partition to submit to

#SBATCH --mem=100 #Memory per node in MB (see also --mem-per-cpu)

#R file to run, and txt files to produce for output and errors
R --vanilla < estimator_performance_simulation.R > runScript_output_%j.txt 2> runScript_errors_%j.txt
