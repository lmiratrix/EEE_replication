#!/bin/bash

#SBATCH -n 2  #Number of cores

#SBATCH -t 800  #Runtime in minutes

#SBATCH -p stats #Partition to submit to

#SBATCH --mem=30000 #Memory per node in MB (see also --mem-per-cpu)

#R file to run, and txt files to produce for output and errors
R --vanilla < runScript.R > runScript_output.txt 2> runScript_errors.txt